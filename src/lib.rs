pub mod configuration;
pub mod routes;
pub mod startup;

// use actix_web::{App, HttpRequest, HttpResponse, HttpServer, Responder, web};
// use actix_web::dev::Server;
// use std::net::TcpListener;


// health_check function -> moved to ./routes/health_check.rs

// Struct FormData -> moved to ./routes/subscriptions

// subscribe handler function -> moved to ./routes/subscriptions

// run function -> moved to ./startup.rs